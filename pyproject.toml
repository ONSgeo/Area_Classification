[tool.ruff.lint]
select = [
    "E",
    "F",
    "B",
    "SIM",
    "I",
]
ignore = ["D203", "E203"]

[tool.ruff]
line-length = 101
exclude = ["tests"]

[tool.ruff.format]
line-ending = "auto"

[tool.bandit]
exclude_dirs = ["tests", "docs"]
skips = []
